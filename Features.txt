- Scene System: # Done
	- Scene interface, that manages drawing and updating to keep games clean
	- Scene manager that controls variables that switch in between scenes. Can be editted to have constants in between scenes
	- Framerate counter within scene manager, that has on the fly FPS calculations

- TODO:
- Raycast (sin + cos = rot, dist, steps in between dist and center, rects to collide with other rects)
- GUI system
- Camera system possibly